{
    "name": "{{ project_name }}_colab",
    "version": "{{ version }}",
    "description": "{{ description }}",
    "author": "{{ author }}",
    "license": "{{ license }}",
    "keywords": [
        "colab",
        "widgets",
        "{{ project_name }}"
    ],
    "scripts": {
        "build": "webpack",
        "sdist": "rm -rf dist && python setup.py sdist",
        "upload": "twine upload dist/*"
    },
    "dependencies": {
    },
    "devDependencies": {
        "webpack": "^4.35.0",
        "webpack-cli": "^3.3.6"
    }
}
